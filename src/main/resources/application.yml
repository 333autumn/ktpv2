server:
  port: 8080

# mysql配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ktp?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    #台式机mysql密码
#    password: 123456
   # 笔记本mysql密码
    password: root

#开启mybatis-plus控制台 SQL 日志打印
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
